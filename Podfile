# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'XCTestSample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for XCTestSample
  pod 'SwiftyJSON'
  pod 'Alamofire'

  target 'XCTestSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'XCTestSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
